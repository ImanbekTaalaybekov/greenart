# 📬 Подробное описание запросов для Postman

*В этом документе запросы оформлены простыми текстовыми блоками (без Markdown-таблиц), так как таблицы криво отображаются в описании Postman. Скопируйте этот текст и вставьте в поле Description каждого соответствующего запроса.*

---

## 🆕 НОВЫЕ ЗАПРОСЫ (ДОБАВЬТЕ ЭТИ ЗАПРОСЫ В POSTMAN В АДМИН ПАНЕЛЬ)

### 1. Одобрение визита
**Метод / Путь:** `POST /api/admin/visits/{visit}/approve`
**Описание:** Подтверждает, что садовник реально присутствовал на визите (устанавливает поля `approved_at`, `approved_by`).
**Path-параметры:**
- `{visit}` (integer) — ID визита
**Тело запроса:** Нет
**Ответ:** 200 OK, объект визита `WorkVisit`.

### 2. Редактировать визит
**Метод / Путь:** `PATCH /api/admin/visits/{visit}`
**Описание:** Админ может изменить данные визита (например, если садовник ошибся датой или не тот заказ указал).
**Параметры в теле (JSON):**
- `order_id` (integer) [Опционально] — ID заказа 
- `worker_id` (integer) [Опционально] — ID садовника
- `visit_date` (date) [Опционально] — Новая дата визита
- `comment` (string) [Опционально] — Новый комментарий
**Ответ:** 200 OK, обновлённый визит.

### 3. Зарплаты всех сотрудников за период
**Метод / Путь:** `GET /api/admin/salaries`
**Описание:** Возвращает массив со всеми садовниками, у которых за указанный период были отработанные дни, штрафы или надбавки, и их суммарную зарплату.
**Query-параметры:**
- `from` (date) [Обязательно] — Начало периода (гггг-мм-дд)
- `to` (date) [Обязательно] — Конец периода (гггг-мм-дд)
**Ответ:** 200 OK, объект `{ period: {from, to}, workers: [ ... ] }`.

### 4. Список штрафов и надбавок
**Метод / Путь:** `GET /api/admin/salary-adjustments`
**Описание:** Просмотр списка всех или отфильтрованных штрафов и надбавок (премий).
**Query-параметры:**
- `worker_id` (integer) [Опционально] — Фильтр по садовнику
- `from` (date) [Опционально] — От даты
- `to` (date) [Опционально] — До даты
- `type` (string) [Опционально] — Тип: `penalty` или `bonus`
**Ответ:** 200 OK, пагинация моделей `SalaryAdjustment`.

### 5. Создать штраф или надбавку
**Метод / Путь:** `POST /api/admin/salary-adjustments`
**Описание:** Добавить садовнику штраф или премию, которые учтутся при расчёте ЗП.
**Параметры в теле (JSON):**
- `worker_id` (integer) [Обязательно] — Кому
- `type` (string) [Обязательно] — `penalty` (штраф) или `bonus` (премия)
- `amount` (numeric) [Обязательно] — Сумма
- `date` (date) [Обязательно] — К какому числу относится (гггг-мм-дд)
- `reason` (string) [Опционально] — Комментарий (за что)
**Ответ:** 201 Created, объект модели.

### 6. Удалить штраф/надбавку
**Метод / Путь:** `DELETE /api/admin/salary-adjustments/{adjustment}`
**Описание:** Удаляет штраф или премию, если она выставлена ошибочно.
**Path-параметры:**
- `{adjustment}` (integer) — ID штрафа/надбавки
**Ответ:** 200 OK.

### 7. Классификация заказа админом
**Метод / Путь:** `PATCH /api/admin/orders/{order}/classification`
**Описание:** После того как клиент отправил заказ, админ может классифицировать его.
**Параметры в теле (JSON):**
- `payment_type` (string) [Обязательно] — `included` (по подписке) или `extra` (за доп. плату)
- `payment_money` (numeric) [Обязательно если extra] — Сумма оплаты для доп. задачи
**Ответ:** 200 OK.

### 8. Список садовников (Админ)
**Метод / Путь:** `GET /api/admin/workers`
**Описание:** Получить список только пользователей-садовников (для выпадающих списков при назначении).
**Ответ:** 200 OK.

### 9. Все пользователи (Админ)
**Метод / Путь:** `GET /api/admin/users`
**Описание:** Просмотр списка всех пользователей.
**Query-параметры:**
- `role` (string) [Опционально] — Фильтр по роли (`client`, `worker`, `admin`, `accountant`)
**Ответ:** 200 OK, пагинация.

### 10. Расписание садовника
**Метод / Путь:** `GET /api/workers/{worker}/schedule`
**Описание:** Возвращает информацию (чаще для календаря), в какие дни работал садовник. Доступно для `admin` и `client` (только для своего садовника).
**Query-параметры:**
- `from` (date) [Опционально] — Начало
- `to` (date) [Опционально] — Конец
- `type` (string) [Опционально] — `included` или `extra`

---

## 🔄 ОСТАЛЬНЫЕ ЗАПРОСЫ (УЖЕ ЕСТЬ В КОЛЛЕКЦИИ POSTMAN)

*(Скопируйте описания ниже и вставьте в соответствующие запросы)*

### Клиент: Регистрация
**Метод / Путь:** `POST /api/register`
**Описание:** Создание нового аккаунта. Токен не нужен.
**Параметры (JSON):**
- `name` (string) [Обязательно] — Имя пользователя
- `login` (string) [Обязательно] — Уникальный логин
- `email` (string) [Обязательно] — Email (уникальный)
- `phone` (string) [Опционально] — Телефон
- `role` (string) [Обязательно] — Роль (`client`, `worker`, `admin`, `accountant`)
- `password` (string) [Обязательно] — Пароль (не менее 8 символов)
- `device` (string) [Опционально] — Название устройства для токена

### Клиент: Вход
**Метод / Путь:** `POST /api/auth`
**Описание:** Авторизация по логину или email.
**Параметры (JSON):**
- `login` (string) [Обязательно] — Логин или email
- `password` (string) [Обязательно] — Пароль
- `device` (string) [Опционально] — Название устройства
**Ответ:** Токен, который нужно передавать в `Authorization: Bearer <token>` во всех остальных запросах.

### Клиент: Профиль
**Метод / Путь:** `GET /api/me`
**Описание:** Возвращает объект текущего авторизованного пользователя.

### Клиент: Обновление профиля
**Метод / Путь:** `PUT /api/user`
**Описание:** Обновление личных данных текущего пользователя.
**Параметры (JSON):**
- `name` (string) [Опционально]
- `login` (string) [Опционально]
- `email` (email) [Опционально]
- `phone` (string) [Опционально]
- `password` (string) [Опционально] — Новый пароль

### Клиент: Выход
**Метод / Путь:** `POST /api/logout`
**Описание:** Уничтожает текущий токен (на устройстве).

---

### Админ: Назначить работника
**Метод / Путь:** `POST /api/admin/orders/{order}/assign-worker`
**Описание:** Назначить садовника на конкретный заказ. Если заказ был `pending`, он перейдет в статус `assigned`.
**Path-параметры:**
- `{order}` (integer) — ID заказа
**Параметры (JSON):**
- `worker_id` (integer) [Обязательно] — ID садовника

### Админ: Закрепить садовника за клиентом
**Метод / Путь:** `POST /api/admin/clients/{client}/default-worker`
**Описание:** Назначить клиенту садовника по умолчанию. Новые заказы клиента будут автоматически падать на этого садовника (при определённой бизнес-логике).
**Параметры (JSON):**
- `worker_id` (integer) [Обязательно] — ID садовника

### Админ: Список клиентов
**Метод / Путь:** `GET /api/admin/clients`
**Описание:** Возвращает список только пользователей с ролью `client`. С пагинацией.

### Админ: Создание пользователя
**Метод / Путь:** `POST /api/admin/users`
**Описание:** Заведение учётной записи (админом).
**Параметры (JSON):**
- То же самое что в регистрации, но без `device`.
- Можно передать `salary` (numeric) для `role = worker`.
- Можно передать `default_worker_id` (integer) для `role = client`.

### Админ: Обновление пользователя
**Метод / Путь:** `PATCH /api/admin/users/{user}`
**Описание:** Админ может изменить данные любого пользователя (например оклад или привязанного садовника).
**Параметры (JSON):**
- `name`, `login`, `email`, `phone`, `password`, `role`.
- `salary` — обновить дневную ставку (рабочему)
- `default_worker_id` — обновить садовника (клиенту)

### Админ: Расчет Зарплаты
**Метод / Путь:** `GET /api/admin/workers/{worker}/salary`
**Описание:** Подсчитывает ЗП конкретного садовника по формуле: `(дни_работы * ставка) + доп_работы + премии - штрафы`.
**Query-параметры:**
- `from` (date) [Обязательно] — с
- `to` (date) [Обязательно] — по

### Админ: Просмотр визитов (Admin)
**Метод / Путь:** `GET /api/admin/visits`
**Описание:** Просматривать визиты (check-ins) садовников.
**Query-параметры:**
- `worker_id` (int) [Опционально] 
- `date` (date) [Опционально] 
- `from` (date) [Опционально] 
- `to` (date) [Опционально] 
- `approved` (boolean `1` или `0`) [Опционально] — скрыть неодобренные / показать только одобренные.

---

### Общие заказы: Все заказы
**Метод / Путь:** `GET /api/orders`
**Описание:** Получение списка заказов. Админ видит всё. Клиент видит свои заказы. Садовник видит назначенные на него.
**Query-параметры:**
- `status` (string) [Опционально] — pending, assigned, in_progress, done, cancelled.
- `worker_id` (int) [Опционально] 
- `client_id` (int) [Опционально]

### Общие заказы: Заказ
**Метод / Путь:** `GET /api/orders/{order}`
**Описание:** Детальная информация по одному заказу (включая фотки и отчёты).

### Общие заказы: Создать заказ
**Метод / Путь:** `POST /api/orders`
**Описание:** Клиент (или админ за клиента) создаёт новый заказ. Можно прикрепить фото.
**Параметры (Multipart/form-data):**
- `description` (text) [Обязательно] — Описание
- `client_id` (integer) [Обязательно] — ID клиента
- `photos[]` (files) [Опционально] — до 100 файлов (картинок)

### Общие заказы: Обновить заказ
**Метод / Путь:** `PUT /api/orders/{order}`
**Описание:** Обновить описание или изменить свойства (Админским доступом). Способен загружать новые фото (массив `photos[]` НЕ удаляет старые, а добавляет сверху - старые удаляются через отдельный метод `DELETE /api/order-photos`).
**Параметры (Multipart/form-data):**
- `description` (text) [Опционально]
- `photos[]` (files) [Опционально]
- `status` (string) [Опционально] — админам
- `payment_money` (numeric) [Опционально] — админам, если `payment_type` = `extra`
- `payment_type` (string) [Опционально] — `included` или `extra`

### Общие заказы: Удалить заказ
**Метод / Путь:** `DELETE /api/orders/{order}`
**Описание:** Клиент может удалить заказ, только если он `pending`. Админ может удалить всегда.

### Общие заказы: Удаление Фото (Order Photo) 
**Метод / Путь:** `DELETE /api/order-photos/{order_photo}`
**Описание:** Удаляет конкретную картинку из заказа.

---

### Садовник: Мои задачи
**Метод / Путь:** `GET /api/worker/tasks`
**Описание:** Заказы (задачи), назначенные на текущего работника.
**Query-параметры:**
- `status` (string) [Опционально] — Фильтр (assigned / in_progress / done / cancelled)
- `type` (string) [Опционально] — Фильтр (included / extra)

### Садовник: Отчёты за дату
**Метод / Путь:** `GET /api/worker/reports`
**Описание:** Получить список своих сданных отчётов за определённую дату. Возвращает отчёты + их фотки.
**Query-параметры:**
- `date` (date) [Обязательно] — Дата
- `type` (string) [Опционально] — included/extra

### Садовник: Отчёт по задаче
**Метод / Путь:** `POST /api/worker/tasks/{order}/report`
**Описание:** Садовник прикрепляет отчёт к задаче. Если передать `is_completed=1` (и прикрепить фотки!), заказ перейдет в статус `done` и будет сохранено время `completed_at`.
**Параметры (Multipart/form-data):**
- `report_date` (date) [Обязательно] — За какое число отчёт (YYYY-MM-DD)
- `comment` (text) [Опционально] — Ревью, текстовое поле
- `is_completed` (boolean `0|1`) [Опционально] — Пометить как полностью выполнено
- `photos[]` (files) [Опционально, но Обязательно если `is_completed=1`] — Фотографии результата работы.

### Садовник: Создать визит [Check-in]
**Метод / Путь:** `POST /api/worker/visits`
**Описание:** Отметка приложения о том, что садовник прибыл на объект по заказу. Позже админ должен `approve` этот визит.
**Параметры (JSON):**
- `order_id` (integer) [Обязательно] — ID заказа, по которому прибыл
- `visit_date` (date) [Обязательно] — Дата (YYYY-MM-DD)
- `comment` (string) [Опционально] — Примечание

### Садовник: Получить список визитов
**Метод / Путь:** `GET /api/worker/visits`
**Описание:** Мои (садовника) текущие добавленные визиты.
**Query-параметры:** `date`, `from`, `to`.

### Садовник: Удаление визита
**Метод / Путь:** `DELETE /api/worker/visits/{visit}`
**Описание:** Садовник может удалить визит (например, если чек-ин был сделан ошибочно и админ его ещё не проверил / не одобрил).

### Садовник: График садовника
**Метод / Путь:** `GET /api/workers/{worker}/schedule`
*(запрос есть в папке "Садовник: График садовника" на скрине, хотя по сути это маршрут админа или клиента, но пусть будет тут)*
**Описание:** Календарь рабочих дат садовника.

---

### Объявления: Список Объявлений
**Метод / Путь:** `GET /api/announcements`
**Описание:** Лента объявлений. Вернутся только те, которые адресованы роли текущего юзера (или `all`).

### Объявления: Детализация Объявления
**Метод / Путь:** `GET /api/announcements/{announcement}`
**Описание:** Детальный просмотр объявления со списком прикреплённых фоток. В эту же минуту можно было бы отметить его "прочитанным".

### Объявления: Создать Объявление
**Метод / Путь:** `POST /api/announcements`
**Описание:** Админ публикует новость/объявление.
**Параметры (Multipart/form-data):**
- `title` (string) [Обязательно] 
- `body` (text) [Опционально]
- `audience` (string) [Обязательно] — `all`, `clients`, `workers`
- `published_at` (date) [Опционально] — Если пусто, будет дата на момент вызова API (now)
- `photos[]` (files) [Опционально] — Поддержка множественной загрузки

### Объявления: Обновить Объявление
**Метод / Путь:** `PUT /api/announcements/{announcement}`
*(в Postman лучше использовать POST запрос, а в теле слать скрытый ключ `_method="PUT"`, если там есть файлы)*
**Описание:** Обновить текущее объявление, поменять заголовок, текст, аудиторию.

### Объявления: Удалить Объявление
**Метод / Путь:** `DELETE /api/announcements/{announcement}`
**Описание:** Удаляет само объявление и связанные картинки/статусы просмотра (только админом).

### Объявления: Удаление фото
**Метод / Путь:** `DELETE /api/announcement-photos/{announcement_photo}`
**Описание:** Удалить одну конкретную фотографию, прикреплённую к объявлению, если её прикрепили ошибочно.

---

### Чаты: Список чатов
**Метод / Путь:** `GET /api/chats`
**Описание:** Чаты в которых юзер состоит. Выведет: название чата, тип, аватарку (через путь storage) и счетчик общего количества сообщений в чате. Сортировка по самым новым изменённым чатам.

### Чаты: Создание чата
**Метод / Путь:** `POST /api/chats`
**Описание:** Юзер с правами `admin` создаёт новый чат.
**Параметры (Multipart/form-data):**
- `title` (string) [Обязательно] — Название чата
- `type` (string) [Опционально] — `general` или `order` (дефолт: order)
- `participants[]` (array of integer) [Обязательно] — ID юзеров для чата. Админ, который вызывает запрос, добавится в этот чат автоматически, его ID можно не передавать.
- `avatar` (file) [Опционально] — Обложка чата

### Чаты: Переписка чата
**Метод / Путь:** `GET /api/chats/{chat}/messages`
**Описание:** Загрузить список сообщений по `chat_id`. 50 сообщений за страницу. Показывает инфу по каждому сообщению: кто отправил, текст, путь к вложенному файлу и кто уже прочитал это сообщение.

### Чаты: Отправка сообщения
**Метод / Путь:** `POST /api/chats/{chat}/messages`
**Описание:** Создать новое сообщение в чате. Отправка инициирует событие Websocket.
**Параметры (Multipart/form-data):**
- `message` (string) [Опционально]
- `file` (file) [Опционально] - максимум 10 Мегабайт
*(Хотя бы одно из двух полей обязательно должно быть передано)*

### Чаты: Статус прочтения
**Метод / Путь:** `POST /api/chats/{chat}/read`
**Описание:** Вызывается при активности в чате, чтобы пометить все входящие (чужие) сообщения от других пользователей как "прочитано мной". Полезно, чтобы на фронте убирать счетчик непрочитанных.

### Чаты: Изменить чат
**Метод / Путь:** `POST /api/chats/{chat}/update`
**Описание:** Поменять название, иконку, описание текущего чата (Админом). Обратите внимание: используется POST из-за того что в теле может быть `avatar` File.
**Параметры (Multipart/form-data):**
- `name` (string) [Опционально]
- `description` (string) [Опционально]
- `avatar` (file) [Опционально]

### Чаты: Удалить чат
**Метод / Путь:** `DELETE /api/chats/{chat}`
**Описание:** Удаление объекта чата и всех его сообщений навсегда. (Только Админ).
